// Dependencies
import styled from 'styled-components';
import { Dialog } from '../../Common';

export const CrateNewClientDialog = styled(Dialog)`
  .MuiDialog-paper {
    max-width: 1320px !important;
  }
`;
